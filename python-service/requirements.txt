# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Image processing
Pillow>=10.3.0
colorthief>=0.2.1

# Face landmark detection (Tasks API — compatible with 0.10.14+)
# The face_landmarker.task model (~1.3 MB) is auto-downloaded on first use
mediapipe>=0.10.14

# CLIP embeddings (downloads ~350 MB model on first run)
sentence-transformers>=3.0.0

# Async HTTP (for fetching hairstyle images without stored embeddings)
httpx>=0.27.0

# Utilities
numpy>=1.26.0
python-dotenv>=1.0.0
